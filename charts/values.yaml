replicaCount: 2

image:
  repository: ksreenath2027/simple-app
  tag: 70f8ac5
  pullPolicy: Always

service:
  type: ClusterIP
  port: 5002

containerPort: 5002
ingress:
  enabled: true
  className: nginx

  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"

  hosts:
    - host: myapp.test
      paths:
        - path: /
          pathType: Prefix

  tls: []