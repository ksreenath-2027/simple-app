apiVersion: v1
kind: Service
metadata:
  name: simple-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: simple-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}